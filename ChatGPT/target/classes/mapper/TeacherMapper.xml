<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huilian.mapper.TeacherDao">

    <select id="selectDaFenCount" resultType="java.lang.Integer">

        SELECT count(1) from (SELECT s from student) as s
                                 LEFT JOIN
                                 (SELECT stuid from stu_source where teaid = 64) as b
        where s.id not in b.stuid
    </select>
</mapper>
